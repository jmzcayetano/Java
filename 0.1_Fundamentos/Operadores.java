public class Operadores {

    /*
     * Operadores
     * 
     * Objetivo: Usar operadores para realizar operaciones aritméticas, lógicas, y
     * de comparación.
     * Conceptos:
     * Operadores aritméticos: +, -, *, /, %.
     * Operadores de comparación: ==, !=, <, >, <=, >=.
     * Operadores lógicos: &&, ||, !.
     * Operadores de asignación: =, +=, -=, *=, /=.
    */
    int a = 10;
    int b = 20;

    int suma = a + b;
    boolean esMayor = a > b;// false

}
