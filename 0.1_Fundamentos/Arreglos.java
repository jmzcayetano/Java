public class Arreglos {

    /*
     * Declaración e Inicialización de Arreglos
     * 
     * Concepto: Un arreglo es una colección de elementos de un tipo específico,
     * organizados de manera secuencial en memoria.
     * Sintaxis:
     * Puedes declarar un arreglo especificando el tipo de datos seguido de [].
     * La inicialización se puede hacer al momento de la declaración o
     * posteriormente.
    */

    // Declaración e inicialización al mismo tiempo
    int[] numeros = { 1, 2, 3, 4, 5 };

    // Declaración y luego inicalización
    String [] nombres;
    nombres=new String[3];
    nombres[0]="Monica";
    nombres[1]="Carlos";
    nombres[2]="Luis";

}
